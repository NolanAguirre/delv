
let manager = new QueryManager()

describe('Query Manager test', () => {
    it('initializes', () => {
        
    })

    it('creates query', () => {

    })

    it('emits all the correct events', () => {

    })

    it('reuses a query', () => {

    })
})
